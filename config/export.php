<?php
// config/export.php

return [
    /*
     * На каком диске (filesystem disk) сохранять экспорт.
     * Мы заведём диск 'export', корнем которого станет папка docs/
     */
    'disk'   => 'export',
    'routes' => [
        '/',            // новая «главная»
        '/login',
        '/register',
        // '/books', если нужно отдельное URL
    ],
    /*
     * Перечень маршрутов, которые нужно экспортировать в статику.
     * Укажи здесь **все** урлы своего приложения
     */
    'routes' => [
        '/',                   // главная страница (HomeController@index или /welcome)
        '/home',               // если есть убеждение на /home
        '/login',              // форма логина
        '/register',           // форма регистрации
        '/password/reset',     // запрос сброса пароля
        '/password/reset/*',   // страница ввода нового пароля (звёздочка чтобы поймать токен)
        // === Книги ===
        '/books',              // список книг
        '/books/create',       // форма создания книги
        '/books/test',         // если у тебя есть тестовая view test.blade
        // (динамические страницы /books/{id}/edit и /books/{id} нельзя экспортировать без указания конкретных id)
    ],

    /*
     * Хуки до/после экспорта (обычно не трогаем)
     */
    'before' => [],
    'after'  => [],
];
